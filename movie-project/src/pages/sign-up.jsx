import styled from "styled-components";

const Container = styled.div`
    color: white;
`

const SignUp = () => {
    return (
        <Container>
            <div>회원가입</div>
        </Container>
    );
};

export default SignUp;