import styled from "styled-components";

const Container = styled.div`
    color: white;
`

const Login = () => {
    return (
        <Container>
                <div>로그인!</div>
        </Container>
    );
};

export default Login;