import styled from "styled-components";

const Search = () => {
    return (
        <Container>
            <div>검색!</div>
        </Container>
    );
};

export default Search;

const Container = styled.div`
    color: white;
`